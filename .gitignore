# Vercel
.vercel

# Dependencies (only if a build step is ever added)
node_modules/

# OS / editor noise
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# Local secrets — never commit these
.env
.env.*
!.env.example
